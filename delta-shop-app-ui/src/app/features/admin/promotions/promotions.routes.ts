import { Routes } from '@angular/router';
import { AdminPromotionsComponent } from './promotions.component';

export const PROMOTIONS_ROUTES: Routes = [
  { path: '', component: AdminPromotionsComponent }
];
